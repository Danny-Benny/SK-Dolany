export interface DiscussionsSelectorProps {
  title: any;
}

export interface Groups {
  username: string;
  discussion_id: number;
  group_id: number;
  author_id: number;
  topic: string;
}
